#!/usr/bin/with-contenv bashio

mkdir -p /media/Downloads/Incomplete
mkdir -p /media/Downloads/Incoming
mkdir -p /share/rtorrent/.session

rm -f /share/rtorrent/.session/rtorrent.lock

chmod +x /usr/bin/rtorrent.sh
chmod +x /usr/bin/flood.sh
chmod +x /usr/bin/rtunrar.sh
